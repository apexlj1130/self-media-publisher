# 自媒体双平台发布 Skill 离线验收报告

验收日期：2026-09-17

## 验收结论

`self-media-publisher` 已完成离线实现、精确归档复验和个人安装。统一内容包、移动端无序列表、双平台运行包校验、微信官方 API 请求契约、小红书安全浏览器规则、分平台确认和隐私边界均通过当前范围内的自动化或可复查证据。

真实 AI 生图、微信 API 草稿、小红书浏览器草稿、两端页面预览和正式发布均未执行。这些是需要当前素材和当次授权的外部验收边界，不能由离线测试替代。

## 候选制品

- Skill 源目录：`outputs/self-media-publisher/`
- 归档：`outputs/self-media-publisher.tar.gz`
- 归档大小：30,493 bytes
- 归档 SHA-256：`e75e8425b6eeb39c55aae56ec20b79e3f889faf09fca5e84771d2ba960e17705`
- 归档文件数：17
- 安装目录：`/Users/lee/.codex/skills/self-media-publisher`
- 微信配置：`/Users/lee/.config/self-media-publisher/wechat.yaml`，权限 `0600`
- 旧安装备份：`/Users/lee/.codex/skills/wechat-article-publisher.backup-20260917-190453`
- Python：3.9.6
- 实现基线提交：`5e8ba76`（统一工作流）
- 远程仓库：`https://github.com/apexlj1130/wechat-article-publisher`

## 自动化证据

### 源目录

```bash
python3 -m unittest discover -s outputs/self-media-publisher/tests -p 'test_*.py' -v
python3 /Users/lee/.codex/skills/.system/skill-creator/scripts/quick_validate.py outputs/self-media-publisher
```

结果：25/25 测试通过；官方校验输出 `Skill is valid!`。

### 精确归档

使用 `COPYFILE_DISABLE=1` 从 `outputs/` 只归档 `self-media-publisher/`。归档解压到新建的 `/tmp/self-media-publisher-verify.vSlIam` 后重新执行完整测试和官方校验。

结果：25/25 测试通过；官方校验通过；解压得到 17 个文件。

### 已安装副本

- `diff -qr` 证明安装目录与候选源码递归一致。
- 安装副本 25/25 测试通过。
- 安装副本官方校验通过。
- 安装后的 `wechat_draft.load_config()` 能读取迁移配置，且没有输出账号凭证。

## 验证覆盖

- 统一内容包 schema v2，默认平台为微信和小红书。
- 合成文章解析为 5 个图片任务，章节语义锚点保持稳定。
- 无序列表项目符号和正文位于同一个 `li` 流，不使用导致手机端异常空白的独立项目符号块。
- 媒体清单拒绝源指纹不符、图片缺失、相对路径和不存在文件。
- 小红书包拒绝标题、正文、标签、图片、可见范围和原创选项越界，并生成稳定指纹。
- 微信配置拒绝缺失凭证和非私有权限；配置对象表示不泄露 secret。
- fake transport 证明 token、永久封面、正文图、`draft/add`、`draft/get` 的方法、路径和负载。
- `40164` 不重试；token 读取 5xx 有界重试；上传和草稿创建状态未知时不自动重试。
- 草稿回读标题或图片不一致时失败，且不创建第二份草稿。
- 小红书流程明确禁止私有 API、Cookie、xsec token、Headless、CDP、调试器权限、脚本注入、Shadow DOM 穿透和指纹伪装。
- 两个平台的确认和结果状态互相独立。

## 隐私证据

- Skill 只包含标注为合成内容的文章和配置示例。
- 原始用户文章 SHA-256 黑名单扫描无匹配。
- 对现有微信配置中的 2 个 `app_secret` 值做候选源码逐字节扫描，匹配数为 0；扫描过程没有输出 secret。
- 结构测试拒绝真实配置文件名、token 缓存、Cookie 文件、运行收据和非占位 `app_secret` 值。
- `work/` 已加入 `.gitignore`；真实运行资料、上游仓库和凭证不进入提交。

## 未执行边界

- 未调用 Codex `imagegen` 生成本次真实平台图片。
- 未请求微信真实 access token，未上传永久素材，未创建真实公众号草稿。
- 未打开小红书创作平台，未上传图片或保存真实小红书草稿。
- 未在微信后台勾选原创声明或做手机预览。
- 未执行任何正式发布。

下一阶段应分两次授权在线验收：先创建并检查微信 API 草稿，再使用可见 Chrome 填充并检查小红书草稿。正式发布仍需每个平台当前版本的明确确认。
